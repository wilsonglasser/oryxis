[package]
name = "oryxis-sync"
version = "0.1.0"
edition = "2024"

[dependencies]
oryxis-core = { workspace = true }
# iroh — will be re-added when P2P sync is implemented
tokio = { workspace = true }
serde = { workspace = true }
tracing = { workspace = true }
thiserror = { workspace = true }
